/**
 * Video Element Creator
 * @param {HTMLVideoElement} element
 * @param {string} url
 */
export default createVideoElement = (element, url) => {
  window.localConnection = localConnection = new RTCPeerConnection(
    servers,
    pcConstraint
  );
};
